// ============================================================
// ProductGrid — Dev-Cosmic wrapper (legacy compatibility shim)
// The catalog logic has been promoted to StorefrontPage.jsx.
// This shim is preserved so any existing imports / tests that
// reference 'components/ProductGrid' continue to resolve.
// ============================================================
import StorefrontPage from './StorefrontPage';
export default StorefrontPage;
